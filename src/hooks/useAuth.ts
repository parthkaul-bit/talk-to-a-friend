// import { useAuth as useClerkAuth } from '@clerk/nextjs';

// export const useAuth = () => {
//   const { isSignedIn, userId, sessionId } = useClerkAuth();
//   return { isSignedIn, userId, sessionId };
// };
